# 个人科技风动态网站开发文档（Vercel 部署）

## 1\. 项目概述

### 1\.1 项目简介

本项目为**科技风轻量化个人静态网站**，纯前端实现、无后端服务，主打**按时间自动切换四套科技主题** \+ 全套轻量化动态动画特效，支持响应式全设备适配，最终部署于 Vercel 并绑定自有国内域名 lengmeng\.online。网站包含主页、关于我、项目作品、博客、日志、相册、联系方式完整模块，集成访客统计、实时天气、动态语录、后台静音音乐、暗黑模式全套拓展功能。

### 1\.2 开发 \& 部署核心定位

- 技术方案：原生 HTML5 \+ CSS3 \+ JavaScript（轻量化、零重型框架、极致适配Vercel）

- 视觉风格：全站统一科技风 / 赛博微光 / 粒子光影 / 网格未来感

- 核心特性：时分四阶段自动换肤、无缝过渡动画、全局视差与入场动效

- 部署方式：Vercel 自动构建部署 \+ 自定义国内域名解析 \+ 全站 HTTPS

- 体验标准：首屏 ≤2s、全设备响应式、无卡顿掉帧、主流浏览器全兼容

## 2\. 整体技术方案

### 2\.1 技术栈明细

- 结构：HTML5 语义化标签，规范分层，利于SEO与维护

- 样式：CSS3 \+ Flex \+ Grid \+ 渐变/发光/阴影动画 \+ 媒体查询响应式

- 交互：原生 JavaScript ES6\+，实现时序换肤、动态特效、拓展功能

- 动画方案：纯CSS动画 \+ 轻量JS逐帧控制，不引入jQuery、大型动画库，保证加载速度

- 部署：Vercel 静态站点托管，关联Git仓库持续集成

### 2\.2 项目目录结构（最终代码结构）

```Plain Text
/
├── index.html                # 首页
├── about.html                # 关于我页面
├── works.html                # 项目作品页面
├── blog.html                 # 博客页面
├── log.html                  # 日志页面
├── album.html                # 相册页面
├── contact.html              # 联系方式页面
├── css/
│   ├── common.css            # 全局通用样式、重置、布局、响应式
│   ├── animate.css           # 全局动画库、入场/悬浮/过渡动效
│   ├── theme.css             # 四时科技主题样式（凌晨/上午/下午/夜晚）
│   └── style.css             # 页面主体样式
├── js/
│   ├── theme.js              # 时序自动换肤核心逻辑
│   ├── animate.js            # 滚动入场、视差、悬浮动画
│   ├── weather.js            # 实时天气功能
│   ├── music.js              # 后台静音播放音乐功能
│   ├── sentence.js           # 动态语录
│   ├── visitor.js            # 访客统计
│   ├── darkmode.js           # 暗黑模式切换
│   └── main.js               # 全局初始化、页面统一逻辑
├── images/                   # 网站图片、头像、相册、作品素材
├── music/                    # 背景音乐资源
└── vercel.json               # Vercel部署配置文件
```

## 3\. 核心功能开发逻辑

### 3\.1 四时动态换肤核心（核心特色）

系统读取用户本地实时时间，自动匹配对应主题，刷新页面、实时时间跳转均自动适配，切换搭载无缝融屏过渡动画，无闪屏、无卡顿。

#### 3\.1\.1 主题规则

- **凌晨 00:00–06:00｜深空科技主题**：纯黑深灰底色，蓝紫冷调微光，星空粒子\+深空射线动态背景，静谧未来感

- **上午 06:00–12:00｜晨光科技主题**：浅灰通透底色，青蓝亮渐变色，轻量化网格动态柔光背景

- **下午 12:00–18:00｜白昼科技主题**：高亮浅底，冰蓝\+浅青通透配色，悬浮点阵\+动态线条背景

- **夜晚 18:00–24:00｜赛博科技主题**：深黑底色，霓虹蓝/冰紫渐变光影，流光线条\+全息发光特效

#### 3\.1\.2 切换逻辑

页面初始化执行时间判断 → 挂载对应主题class → 实时时间监听，到达节点自动触发主题切换动画 → 更新全局配色、背景、发光参数、文字色系。

### 3\.2 全局动画系统

所有动画轻量化、可复用、适配移动端，统一动画曲线与时长，保证整体高级统一。

- **页面加载动画**：科技线条扫描、光点流转渐变加载，完成后平滑入场主页

- **滚动视差动画**：背景粒子、网格、页面模块分层视差滚动，强化立体空间感

- **元素悬浮动效**：按钮、卡片、链接 hover 触发微光描边、微缩放、发光扩散

- **模块入场动画**：滚动可视区触发淡入\+上移入场，搭配光影扫过特效

- **主题切换动画**：融屏渐变、粒子扩散、光影流转过渡

### 3\.3 页面模块开发规范

所有页面统一头部导航、底部页脚、全局动画、主题体系，保证全站风格统一。

- **首页**：头像、昵称、个人slogan、动态时序背景、实时天气、动态语录、快捷入口

- **关于我**：个人简介、科技风可视化技能标签、经历展示

- **作品项目**：科技风卡片布局，悬浮发光、视差效果，展示项目案例

- **博客/日志**：图文列表布局，入场动画、hover交互

- **相册**：网格响应式相册，图片缩放、光影悬浮特效

- **联系方式**：社交链接、邮箱、二维码展示，科技风交互反馈

## 4\. 拓展功能开发细则

- **暗黑模式**：独立切换按钮，记忆用户偏好，适配四时主题，全局色系同步切换

- **访客统计**：前端轻量化统计，展示访问人次，数据本地持久化

- **动态语录**：首页随机/轮播科技风文案语录，定时切换

- **后台音乐播放**：全局常驻音乐控件，默认静音自动播放，支持手动开关、切歌

- **实时天气**：获取本地IP对应城市天气，展示温度、天气状况，UI适配科技风

## 5\. 适配与兼容性规范

- 响应式断点：适配手机、平板、桌面1920大屏，布局自动适配、卡片自动重排

- 性能约束：首屏资源精简，动画开启硬件加速，杜绝卡顿、重排闪烁

- 浏览器兼容：Chrome / Edge / Safari / 微信内置浏览器 全兼容

## 6\. Vercel 部署与域名配置方案

### 6\.1 部署流程

1. 项目根目录配置 vercel\.json 适配静态站点规则

2. 推送代码至Git仓库，Vercel关联仓库开启自动部署

3. 自动开启HTTPS、全球加速、缓存策略

### 6\.2 自定义域名配置

支持绑定自有国内域名 **lengmeng\.online**，开发阶段预留域名适配逻辑，部署后提供完整域名解析、DNS配置、HTTPS证书生效教程，同时保留Vercel默认二级域名备用。

## 7\. 交付规范

- 完整结构化源码，代码注释齐全、命名规范、可直接运行

- 所有时序主题、动画、拓展功能100%落地可用

- 全设备响应式适配完成，无布局错乱、无特效异常

- 配套部署文档、域名配置文档、后期修改维护说明

> (Note: May contain AI-generated content.)
